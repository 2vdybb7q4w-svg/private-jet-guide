# THEN / NOW

A transparent historical-investment simulator prototype. The bundled development adapter is deliberately labelled **DEMO DATA**; it must not be used for financial decisions.

## Run

```bash
npm test
npm start
```

Open `http://localhost:4173`. Copy `.env.example` to `.env` when implementing server-side verified providers. API secrets must never be exposed to browser code.
